const router = require('express').Router()

router.post('/api/array/concat', function (request, response) {
  const requestObject = request.body
  //start function call here to concat
})

module.exports = router